// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpgradeBuildingArgs {
    pub building_id: u64,
    pub new_level: u32,
}

impl From<UpgradeBuildingArgs> for super::Reducer {
    fn from(args: UpgradeBuildingArgs) -> Self {
        Self::UpgradeBuilding {
            building_id: args.building_id,
            new_level: args.new_level,
        }
    }
}

impl __sdk::InModule for UpgradeBuildingArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upgrade_building`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upgrade_building {
    /// Request that the remote module invoke the reducer `upgrade_building` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upgrade_building:upgrade_building_then`] to run a callback after the reducer completes.
    fn upgrade_building(&self, building_id: u64, new_level: u32) -> __sdk::Result<()> {
        self.upgrade_building_then(building_id, new_level, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `upgrade_building` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upgrade_building_then(
        &self,
        building_id: u64,
        new_level: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upgrade_building for super::RemoteReducers {
    fn upgrade_building_then(
        &self,
        building_id: u64,
        new_level: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpgradeBuildingArgs {
                building_id,
                new_level,
            },
            callback,
        )
    }
}
