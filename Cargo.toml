[package]
name = "vast"
version = "0.1.0"
edition = "2021"

[workspace]
exclude = ["spacetimedb"]
members = ["universe", "explorer", "vast-bindings"]

[dependencies]
spacetimedb-sdk = { version = "2.1.*" }
vast-bindings = { path = "vast-bindings" }
